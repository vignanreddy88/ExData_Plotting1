library(datasets)
hist(hpc_2$Global_active_power, col = "red", xlab = "Global Active power (Kilowatts)", main = "Global Active Power") dev.copy(png, file = "plot1.png")
 
dev.copy(png, file = "plot1.png")

dev.off()